/**
 * @fileoverview Tests for ESLint Tester
 * @author Nicholas C. Zakas
 */
"use strict";

/*global describe, it*/

//------------------------------------------------------------------------------
// Requirements
//------------------------------------------------------------------------------

var rewire = require("rewire"),
    sinon = require("sinon"),
    eslint = require("eslint").linter,
    ESLintTester = rewire("../../lib/eslint-tester"),
    path = require("path"),
    assert = require("chai").ass